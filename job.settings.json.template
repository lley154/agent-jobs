{
  "version": 1,
  "name": "<Job name shown to the agent>",
  "description": "<one line: what this job watches or does>",
  "program": {
    "linux": "run.sh",
    "macos": "run.sh",
    "windows": "run.cmd"
  },
  "args": [],
  "settings": {
    "_comment": "Program-defined options. The job reads this `settings` block from its own working directory; the calling agent does not interpret it. Add whatever keys your program understands."
  },
  "init": {
    "_comment": "OPTIONAL one-time setup the agent runs before first use. Remove this block if the job needs none. See README.md → One-time initialization.",
    "requires": [],
    "steps": [
      {
        "id": "install",
        "title": "Install dependencies",
        "run": { "linux": "npm install", "macos": "npm install", "windows": "npm install" }
      }
    ],
    "verify": {}
  }
}
