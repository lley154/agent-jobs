# realtor.ca credentials are NOT normally required — public postal-code search
# works anonymously. These are only used if realtor.ca ever forces a login wall.
# Copy this file to `.env` (which is gitignored) and fill in if needed.

REALTOR_USER=
REALTOR_PASS=
